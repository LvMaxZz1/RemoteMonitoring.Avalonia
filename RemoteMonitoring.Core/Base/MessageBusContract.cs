namespace RemoteMonitoring.Core.Base;

public class MessageBusContract
{
    public const string MessageBusService = nameof(MessageBusService);
    
    public const string MessageBusClient = nameof(MessageBusClient);
    
    public const string MessageBusConsole = nameof(MessageBusConsole);
}